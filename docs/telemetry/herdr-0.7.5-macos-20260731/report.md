# Herdr probe report

`herdr-0.7.5-macos-20260731` — generated by `scripts/probe-herdr.sh`

**18 pass, 1 fail, 12 unknown, 3 informational.**

## Failures — these are the ones to act on

### D1 — `events.subscribe` accepts a subscription with no `pane_id` filter

- **observed:** rejected: {"id":"","error":{"code":"invalid_request","message":"invalid request: missing field `pane_id` at line 1 column 196"}}
- **code:** `crates/ansible-herd/src/herdr.rs:Events::spawn`
- **evidence:** `raw/events-wildcard.ndjson`

## Unanswered

Not failures — the probe could not reach these.

- **D3** pushed events name themselves in a `type` field — no events arrived — nothing changed during the window
- **E3** a reported token shows up in the Agents sidebar without extra config — skipped (non-interactive)
- **I3** the stream ends with a `terminal.closed` record — not seen — the probe was killed by its own timeout, which is not the close path
- **I4** observing does not steal input, scroll, or focus from the pane's owner — skipped (non-interactive)
- **J1** `pane.send_text` types without submitting — skipped (needs a human and an agent pane)
- **K2** plugin host contract check K2 — skipped (did not link)
- **K3** plugin host contract check K3 — skipped (did not link)
- **K4** plugin host contract check K4 — skipped (did not link)
- **K5** plugin host contract check K5 — skipped (did not link)
- **C2** a real permission prompt becomes `blocked`, and how fast — skipped (needs a human and an agent pane)
- **L1** the spacetime CLI is available to publish the ported module — spacetime not on PATH — this half is unvalidated
- **L2** the TypeScript module publishes with snake_case names — skipped (no spacetime CLI)

## Everything, in order

| id | area | status | assumption | observed |
|---|---|---|---|---|
| A1 | socket | pass | the socket is at the documented path ($HERDR_SOCKET_PATH, else $XDG_CONFIG_HOME/herdr[/sessions/<name>]/herdr.sock) | found via HERDR_SOCKET_PATH at /Users/marsh/.config/herdr/herdr.sock |
| A2 | socket | pass | a response echoes the request id, and the payload is under `result` | result.type=pong |
| A3 | socket | pass | `ping` reports `version` and a numeric `protocol` | version=0.7.5 protocol=17 capabilities={"detached_server_daemon": true, "live_handoff": true} |
| A4 | socket | pass | an error comes back as `error.code` plus `error.message` | code=invalid_request |
| B1 | snapshot | pass | `session.snapshot` exists and takes empty params | result.type=session_snapshot |
| B2 | snapshot | pass | every field name the parsers probe for is present under its first-choice name | no misses |
| C1 | status | pass | `agent_status` is one of idle\|working\|blocked\|done\|unknown | agent_status=done,agent_status=idle,agent_status=working |
| B3 | snapshot | pass | `agent.explain` reports the matched rule, so a wrong status is diagnosable | captured |
| D1 | events | fail | `events.subscribe` accepts a subscription with no `pane_id` filter | rejected: {"id":"","error":{"code":"invalid_request","message":"invalid request: missing field `pane_id` at line 1 column 196"}} |
| D2 | events | pass | a per-pane subscription is accepted, so the daemon can subscribe per pane instead | {"id":"probe-sub2","result":{"type":"subscription_started"}} |
| D4 | events | info | which event types require a `pane_id` | 1 of 8 types rejected an unfiltered subscription — see the audit |
| D3 | events | unknown | pushed events name themselves in a `type` field | no events arrived — nothing changed during the window |
| E1 | tokens | pass | `pane.report_metadata` accepts a token patch with source, ttl_ms and seq | result.type=ok |
| E2 | tokens | pass | tokens are readable back on the pane record, so the daemon can diff them | found the token in pane.get |
| E3 | tokens | unknown | a reported token shows up in the Agents sidebar without extra config | skipped (non-interactive) |
| E4 | tokens | pass | a null token value clears the key (how a watcher who left is removed) | cleared |
| F1 | notify | pass | `notification.show` reports whether it was shown, via `result.reason` | reason=disabled shown=false |
| F2 | notify | info | how the host rate-limits five toasts in a row | disabled disabled disabled disabled disabled  |
| G1 | view | pass | `agent.view.set` accepts an attention-first sort from a non-plugin source | active=true |
| G2 | view | pass | `agent.view.clear` with our own source restores the configured sort | cleared |
| I1 | teleport | pass | `terminal session observe` streams newline-delimited records | 1 record(s) in 4s |
| I5 | teleport | info | decoded frame bytes go through the redactor before being written | 0
0 redaction event(s) logged over real session output |
| I2 | teleport | pass | a frame's terminal bytes are base64 under a probed field name | field: bytes |
| I3 | teleport | unknown | the stream ends with a `terminal.closed` record | not seen — the probe was killed by its own timeout, which is not the close path |
| I4 | teleport | unknown | observing does not steal input, scroll, or focus from the pane's owner | skipped (non-interactive) |
| J1 | input | unknown | `pane.send_text` types without submitting | skipped (needs a human and an agent pane) |
| K1 | plugin | pass | the manifest's `min_herdr_version` is not newer than the installed binary | manifest 0.7.5 <= installed 0.7.5 |
| K2 | plugin | unknown | plugin host contract check K2 | skipped (did not link) |
| K3 | plugin | unknown | plugin host contract check K3 | skipped (did not link) |
| K4 | plugin | unknown | plugin host contract check K4 | skipped (did not link) |
| K5 | plugin | unknown | plugin host contract check K5 | skipped (did not link) |
| C2 | status | unknown | a real permission prompt becomes `blocked`, and how fast | skipped (needs a human and an agent pane) |
| L1 | spacetime | unknown | the spacetime CLI is available to publish the ported module | spacetime not on PATH — this half is unvalidated |
| L2 | spacetime | unknown | the TypeScript module publishes with snake_case names | skipped (no spacetime CLI) |

## What to send back

The whole directory. The high-value files:

- `report.md` and `assumptions.jsonl` — this, and its machine-readable form
- `raw/field-audit.txt` — every field name the parsers probe, and which one was real
- `raw/frame-audit.txt` — which field carries terminal bytes in an observe frame
- `raw/session-snapshot.json`, `raw/agent-list.json` — to replace the doc-derived test fixtures
- `raw/observe-shapes.ndjson` — observe records with payloads elided
- `raw/events-wildcard.ndjson` — whether the daemon can subscribe or must poll
- `env.txt` — versions, so a later run diffs meaningfully

Captures are piped through the plugin's redactor when it is built. Skim them anyway before sharing: they are terminal output.
