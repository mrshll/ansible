# Herdr probe report

`herdr-telemetry-20260731-102905` — generated by `scripts/probe-herdr.sh`

**10 pass, 4 fail, 8 unknown, 2 informational.**

## Failures — these are the ones to act on

### A3 — `ping` reports a protocol version as `protocol_version`

- **observed:** absent; whole result: {"capabilities": {"detached_server_daemon": true, "live_handoff": true}, "protocol": 17, "type": "pong", "version": "0.7.5"}
- **code:** `crates/ansible-herd/src/herdr.rs:ping`
- **evidence:** `raw/ping.json`

### B2 — every field name the parsers probe for is present under its first-choice name

- **observed:** 5 required field(s) missing, 0 reached only via a fallback — see the audit
- **code:** `crates/ansible-herd/src/herdr.rs:parse_agent`
- **evidence:** `raw/field-audit.txt`

### D1 — `events.subscribe` accepts a subscription with no `pane_id` filter

- **observed:** rejected: {"id":"","error":{"code":"invalid_request","message":"invalid request: missing field `pane_id` at line 1 column 196"}}
- **code:** `crates/ansible-herd/src/herdr.rs:Events::spawn`
- **evidence:** `raw/events-wildcard.ndjson`

### K5 — a `[[startup]]` hook can leave a detached daemon running after it exits

- **observed:** no daemon got printed
- **code:** `crates/ansible-herd/src/main.rs:cmd_startup`

## Unanswered

Not failures — the probe could not reach these.

- **D3** pushed events name themselves in a `type` field — no events arrived — nothing changed during the window
- **E1** `pane.report_metadata` accepts a token patch with source, ttl_ms and seq — no pane to write to
- **I1** `terminal session observe` streams base64 frames — no pane to observe
- **J1** `pane.send_text` types without submitting — skipped (needs a human and an agent pane)
- **K3** an action's log captures the injected HERDR_* environment — nothing obvious in the log — see raw/plugin-log.txt and raw/action-invoke.txt
- **K4** an overlay pane entrypoint opens and restores focus when it closes — skipped by operator
- **C2** a real permission prompt becomes `blocked`, and how fast — skipped (needs a human and an agent pane)
- **L2** the TypeScript module publishes, and its tables are snake_case — skipped by operator

## Everything, in order

| id | area | status | assumption | observed |
|---|---|---|---|---|
| A1 | socket | pass | the socket is at the documented path ($HERDR_SOCKET_PATH, else $XDG_CONFIG_HOME/herdr[/sessions/<name>]/herdr.sock) | found via default at /home/sam/.config/herdr/herdr.sock |
| A2 | socket | pass | a response echoes the request id, and the payload is under `result` | result.type=pong |
| A3 | socket | fail | `ping` reports a protocol version as `protocol_version` | absent; whole result: {"capabilities": {"detached_server_daemon": true, "live_handoff": true}, "protocol": 17, "type": "pong", "version": "0.7.5"} |
| A4 | socket | pass | an error comes back as `error.code` plus `error.message` | code=invalid_request |
| B1 | snapshot | pass | `session.snapshot` exists and takes empty params | result.type=session_snapshot |
| B2 | snapshot | fail | every field name the parsers probe for is present under its first-choice name | 5 required field(s) missing, 0 reached only via a fallback — see the audit |
| C1 | status | pass | `agent_status` is one of idle\|working\|blocked\|done\|unknown | agent_status=working |
| D1 | events | fail | `events.subscribe` accepts a subscription with no `pane_id` filter | rejected: {"id":"","error":{"code":"invalid_request","message":"invalid request: missing field `pane_id` at line 1 column 196"}} |
| D3 | events | unknown | pushed events name themselves in a `type` field | no events arrived — nothing changed during the window |
| E1 | tokens | unknown | `pane.report_metadata` accepts a token patch with source, ttl_ms and seq | no pane to write to |
| F1 | notify | pass | `notification.show` reports whether it was shown, via `result.reason` | reason=disabled shown=false |
| F2 | notify | info | how the host rate-limits five toasts in a row | disabled disabled disabled disabled disabled  |
| G1 | view | pass | `agent.view.set` accepts an attention-first sort from a non-plugin source | active=true |
| G2 | view | pass | `agent.view.clear` with our own source restores the configured sort | cleared |
| I1 | teleport | unknown | `terminal session observe` streams base64 frames | no pane to observe |
| J1 | input | unknown | `pane.send_text` types without submitting | skipped (needs a human and an agent pane) |
| K1 | plugin | pass | the manifest's `min_herdr_version` is not newer than the installed binary | manifest 0.7.5 <= installed 0.7.5 |
| K2 | plugin | pass | the manifest links cleanly (fields, placements, contexts all accepted) | linked; warnings:  |
| K3 | plugin | unknown | an action's log captures the injected HERDR_* environment | nothing obvious in the log — see raw/plugin-log.txt and raw/action-invoke.txt |
| K4 | plugin | unknown | an overlay pane entrypoint opens and restores focus when it closes | skipped by operator |
| K5 | plugin | fail | a `[[startup]]` hook can leave a detached daemon running after it exits | no daemon got printed |
| C2 | status | unknown | a real permission prompt becomes `blocked`, and how fast | skipped (needs a human and an agent pane) |
| L1 | spacetime | info | the spacetime CLI is available to publish the ported module | spacetime Path: /home/sam/.local/share/spacetime/bin/2.7.0-hotfix3/spacetimedb-cli |
| L2 | spacetime | unknown | the TypeScript module publishes, and its tables are snake_case | skipped by operator |

## What to send back

The whole directory. The high-value files:

- `report.md` and `assumptions.jsonl` — this, and its machine-readable form
- `raw/field-audit.txt` — every field name the parsers probe, and which one was real
- `raw/frame-audit.txt` — which field carries terminal bytes in an observe frame
- `raw/session-snapshot.json`, `raw/agent-list.json` — to replace the doc-derived test fixtures
- `raw/observe-shapes.ndjson` — observe records with payloads elided
- `raw/events-wildcard.ndjson` — whether the daemon can subscribe or must poll
- `env.txt` — versions, so a later run diffs meaningfully

Captures are piped through the plugin's redactor when it is built. Skim them anyway before sharing: they are terminal output.
